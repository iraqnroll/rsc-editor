# rsc-landscape pulls in node-canvas; we replace it with a prebuilt binary via
# the `canvas` override in package.json. Nothing here should ever need a C
# toolchain, so fail loudly rather than silently trying to compile.
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
