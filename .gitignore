node_modules/
dist/
build/
.turbo/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Exported caches produced by the editor (never commit generated game data)
out/
exports/

# Golden-image diffs produced by failing render tests
**/__image_snapshots__/__diff_output__/

# Debug renders written by packages/render's preview test. Regenerated on every
# run; the test asserts geometric properties, not pixel equality, so these are
# for looking at rather than for diffing against.
packages/render/preview/
