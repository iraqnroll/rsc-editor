/**
 * Editor shell. Placeholder -- owned by the `editor-ux` agent.
 *
 * The 3D viewport lives in `src/scene/` and is wired to @rsc-editor/render at
 * integration; keep a placeholder component boundary here so the two can be
 * developed independently.
 */
export function App() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 24 }}>
      <h1>RSC Editor</h1>
      <p>Editor shell not built yet.</p>
    </main>
  );
}
